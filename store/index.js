
export const actions = {
  async nuxtServerInit(vuexCtx, appCtx) {
    // console.log('init ser ver')
    // // console.log(appCtx.req.headers.cookie)
    // // const $auth = appCtx.app.$auth;
    // // console.log('93939393939393'.repeat(4), this.$auth)
    // // this.$auth.storage.setCookie('id_token', '12345')
    // let idToken = this.$auth.storage.getCookie('id_token');
    // // console.log('this is the id_token we had in the coooookies!', idToken);
    // if (!idToken) {
    //   // console.log(appCtx.res.getHeader('Set-Cookie'))

    //   // idToken = 'F.A.K.E.T.O.K.E.N.';
    //   // idToken = await this.$auth.fetchUser()
    //   appCtx.redirect('/login');
    // }
  
    // vuexCtx.commit('auth/SET', ['token', idToken])
    
  }
}